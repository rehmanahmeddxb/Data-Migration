.venv/
venv/
deployment.log
.deploy_state.json
.instance_preserve/
# Local environment file with secrets (NEVER committed)
.env
instance/*.db
instance/*.db-wal
instance/*.db-shm
instance/health_snapshot.json
# Pre-deploy database backups made by the deployer
instance/backups/
v44/*.db
v44/*.db-wal
v44/*.db-shm

__pycache__/
*.py[cod]
*.pyo

config.txt
.cache/
.local/
.virtualenvs/
.ipython/
.bashrc
.profile
.vimrc
.lesshst
*.pyc
# QA scratch harness (local test runs, not part of the app)
.qa/
