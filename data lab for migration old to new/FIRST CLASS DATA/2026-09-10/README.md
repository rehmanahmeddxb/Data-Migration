# FIRST CLASS DATA — dated backup · 2026-09-10

**What this folder is:** the dated backup of the **final old → new v4.4 migration**
run on **2026-09-10**, using the owner's **final old data** (`ahmed_cement.db`,
uploaded 2026-09-10 — the production file as it stood at cutover) and the v4.4
schema template (`NewData/ahmed_cement_v44_fresh.db`).

The current (undated) copy of the result always lives one level up at
`FIRST CLASS DATA/ahmed_cement_migrated.db`. This dated folder keeps this
run's exact artifacts together so it can be restored or audited later, even
after the top-level file is regenerated by a newer migration.

## Contents

| File | Meaning |
|---|---|
| `ahmed_cement_migrated.db` | ✅ **The migrated database** (v4.4 schema + final old data). `RESULT: PASS`, 29,586 rows, no voided/cancelled rows, full index parity. |
| `ahmed_cement_migrated.db.report.txt` / `.report.json` | The migration tool's full report: per-table load, purge counts, verification, value parity, next steps. |
| `AMS_FULL_20260910_migrated.amsdb` | Portable, self-verifying SQLite snapshot of the same database (29,586 rows) — the app's Import/Export Center accepts this directly. |
| `verify_report.json` | `full_db_sync verify` on the snapshot: integrity `ok`, 0 FK violations, `looks_like_ams: true`. |
| `import_report.json` | Record of the `full_db_sync import` (clean_replace) into `AMSCOPY9/instance/ahmed_cement_v44_fresh.db` — verification PASS, 29,586 rows inserted, 0 FK violations. |
| `inputs/ahmed_cement.db` | Byte-exact copy of the **final old data** source used (md5 `74d9f4e7…`). *(local backup — not tracked in git; the tracked copy is `data lab for migration old to new/ahmed_cement.db`)* |
| `inputs/NewData_ahmed_cement_v44_fresh.db` | Byte-exact copy of the v4.4 template used (md5 `79775c52…`). *(same convention)* |

## Numbers (this run)

| Metric | Value |
|---|---|
| Old file (final) | 64 tables / **29,669 rows** (WAL checkpointed by the tool's staging copy; source untouched) |
| Template (v4.4) | 69 tables / 3,558 seeded rows |
| Migrated output | 69 tables / **29,586 rows** (2 fresh admins + 7 old users merged = 9 users) |
| Purged by policy | **93 rows** — 62 voided (`is_void=1`) + 26 cancelled (`entry` CANCEL) + 5 cascaded children |
| Relaxed unique indexes | **none** — `entry.uq_entry_auto_bill_no` was restored (its duplicate bill numbers were voided rows the purge removed) |
| Value parity | 52 tables checked, **0 mismatches** (after the documented user-id remap) |
| Key counts | client 324 · direct_sale 2,757 (+5,114 items) · entry 5,273 · invoice 2,389 · payment 908 · pending_bill 1,718 · booking 430 |
| Loaded into the app | `AMSCOPY9/instance/ahmed_cement_v44_fresh.db` — import verification **PASS**, boot OK, 154/154 app tests green, 75/75 pages render |

## How to restore this exact state

```bash
cd AMSCOPY9
python3 -m full_db_sync import \
    --source "../data lab for migration old to new/FIRST CLASS DATA/2026-09-10/AMS_FULL_20260910_migrated.amsdb" \
    --db instance/ahmed_cement_v44_fresh.db --confirm
rm -f instance/health_snapshot.json     # runbook: reset the data-loss baseline before first boot
```

…or simply copy `ahmed_cement_migrated.db` over `instance/ahmed_cement_v44_fresh.db`
while the app is stopped. The full plan and runbook:
`data lab for migration old to new/MIGRATION_PLAN.md`.
